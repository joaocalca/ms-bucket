server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/microservices?useTimezone=true&serverTimezone=UTC&createDatabaseIfNotExist=true
    username: root
    password: ******
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update

aws:
  accessKeyId: ********************
  secretAccessKey: ****************************************
  region: sa-east-1
  s3:
    buckets:
      customer: bucket-name
